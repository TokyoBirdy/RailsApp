<%= form_for([@post, @post.comments.build]) do |f| %>
	<div class="filed">
		<%= f.label :commenter %><br />
		<%= f.text_field :commenter %>
	</div>
	<div class="filed">
		<%= f.label :body %><br />
		<%= f.text_field :body %>
	</div>
	<div class="actions">
		<%= f.submit %>
		
	</div>
<% end %>
